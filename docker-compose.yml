version: "3.8"

services:
  app:
    build: .
    environment:
    - PYTHONUNBUFFERED=1
    container_name: app-api
    ports:
    - "8000:8000"
    volumes:
    - .:/app
    env_file:
    - .env
    command: ["poetry","run","uvicorn","app:app","--host","0.0.0.0","--port","8000"]